services:
  flask_app:
    container_name: flask_app
    restart: always
    build: ./flask_app
    ports:
      - 5000:5000
    command: gunicorn -w 1 -b 0.0.0.0:5000 wsgi:server
    volumes:
      - ./flask_app:/usr/src/app/
      - ./db:/usr/src/app/db